# Dependencies & build
node_modules/
dist/
dist-ssr/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment (use .env.example — never commit real keys)
.env
.env.local
.env.*.local
.env.development
.env.production
.env.e2e

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vite / tooling
vite.config.ts.timestamp-*.mjs
supabase/.temp/

# Capacitor generated / flavor builds (not source of truth)
android-customer/
android-driver/
ios-customer/
ios-driver/
capacitor.config.shared.ts.bak
mobile-apks/

# Scratch / one-off SQL (canonical schema lives in supabase/migrations/)
batch_*.sql
*.sql.bak

# Lockfile policy: npm (package-lock.json) is canonical — ignore Bun locks
bun.lock
bun.lockb

# Local load / debug scripts
scripts/load-sim-*.mjs

# Lovable / misc leftovers
.lovable/
new.github
