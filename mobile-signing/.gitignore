# Local Play upload keys — NEVER commit these.
*.jks
*.keystore
key.properties
*.pem
passwords.txt

# Firebase / FCM secrets (also covered by root .gitignore)
firebase/google-services*.json
firebase/firebase-service-account.json
firebase/fcm-server-key.txt
