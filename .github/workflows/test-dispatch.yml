name: Test Dispatch
on:
  workflow_dispatch:
    inputs:
      version_code:
        description: "test"
        required: false
        default: "1"
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo hello