name: Apply N-store panel main final
on:
  push:
    paths:
      - 'scripts/apply_n_store_panel_main_final.py'
      - '.github/workflows/apply-n-store-panel-main-final.yml'
  workflow_dispatch:
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/apply_n_store_panel_main_final.py
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/StoreApp.tsx
          git commit -m "feat(store-N): show deliveries box under call panel" || exit 0
          git push
