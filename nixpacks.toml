[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci", "npm ci --prefix plugins/capacitor-mapbox-maps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node scripts/serve-dist.mjs"
